#!/bin/bash

low=$(echo "$(acpi -b | grep -o "\w\+, [0-9]\+" | grep -o "[0-9]\+") < 100" | bc)

if [[ $low == "1" ]] 
then
	i3-nagbar -m "BATTERY LOW" -f "-*-tewi-medium-r-*-*-17-*-*-*-*-*-*-*" &>> /dev/null
fi
